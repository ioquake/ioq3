# Orchestration: Bishop Session 3 (Floor Rendering Artifacts)

**Orchestrated**: 2026-05-10T23:59:07Z  
**Agent**: Bishop (Renderer GPU engineer)  
**Session Reference**: 3  

## Work Summary

Bishop diagnosed and fixed two floor rendering artifacts:

1. **Brush-model lightmap seam** — Fixed in `renderBrushModel()`. The per-stage sampler check was missing for lightmap stages; `stageInfo->usesLightmap` was not checked in the sampler-binding logic. Now lightmap stages correctly use `sceneClampSampler_` (ClampToEdge address mode) instead of repeating, eliminating hard-edged seams around geometry.

2. **Orange dlight splash** — Fixed at all 3 dlight pass binding sites: `encodeDlights()` (world surfaces), animated model dlight pass, and brush model dlight pass. The radial-falloff texture was incorrectly bound with Repeat sampler; UVs outside [0,1] wrapped to bright center of disk, producing full-intensity dlight contribution far beyond intended radius. All 3 sites now use `sceneClampSampler_` (ClampToEdge).

## Commits

- **36d6ab32**: Floor rendering fixes (lightmap + dlight samplers)

## Technical Details

Both fixes are sampler-binding corrections. The lightmap fix guards `useClamp` with `stageInfo->usesLightmap`; the dlight fix ensures all three dlight pass sites use the same ClampToEdge sampler. Both are one-line guard/assignment changes per site.

## Decision Artifact

The dlight sampler decision is now recorded in decisions.md.
